f := 0;
F :: 1

S,Z :: struct {
    x : i32;
    y : f32;
}

fn :: () -> int {
    return 0;
}

main :: () {
    a, b, c := 0, 2, 3;

    fn();


    d := if a {
        100
    } else {
        200
    };

    d = if a then 100 else 200;

    ptr : *int;

    arr : [10]foo;

    foo : u32;

    x, y = 0, 0;
    1 + 1;

    x = 1 + 1;

    (10 + 1).* = 0;

    foo = if a {
        128
    } else if b {
        256
    } else if c {
        512
    };

    if a {
        x = 100;
    } else if b {
        z = 20;
    } else {
        y = 200;
    }
}
